# PR #17 フィードバック修正TODO

## レビュワー: aconitum01
最終更新: 2025-10-31

---

## 🔴 高優先度

### 1. ネットワークスキャンの変更
**ファイル**: `projects/Game/src/network/HostDiscovery.cpp` (line 135)
- **問題**: 現在の実装では隣接する20個のサブネット（±10）をスキャンしている。これはLAN管理者に怒られる可能性がある
- **修正**: ブロードキャストに変更する

### 2. 通信プロトコルの文書化
**ファイル**: `projects/Game/src/network/MultiplayerManager.cpp` (line 226)
- **問題**: プロトコルの仕様が明記されていない
- **修正**: Notionまたはコードコメントまたはその両方にプロトコルの仕様を明記する

### 3. テキストメッセージの送信方法の変更
**ファイル**: `projects/Game/src/network/MultiplayerManager.cpp` (line 493)
- **問題**: 
  - テキストをそのまま送信している
  - 文字列の上限サイズが設けられていない
- **修正**: 
  - テキストをそのまま送らず、イベントIDとその他数値を持ったパケットを送る形式に変更
  - 文字列の上限サイズを設定する

---

## 🟡 中優先度

### 4. 接続タイムアウトの実装
**ファイル**: `projects/Game/src/network/MultiplayerManager.cpp` (line 52)
- **問題**: `connect()` にタイムアウトがない
- **修正**: タイムアウト処理を追加する

### 5. 接続失敗時のフラグリセット
**ファイル**: `projects/Game/src/network/MultiplayerManager.cpp` (line 61)
- **問題**: 次の `isConnected()` の else ブロック内で or の比較を行っているので、`m_clientConnectSucceeded` を false にする必要がある可能性
- **修正**: 接続失敗時に `m_clientConnectSucceeded = false` を追加

### 6. ポート自動検出機能の見直し
**ファイル**: `projects/Game/src/network/HostDiscovery.cpp` (line 258)
- **問題**: LAN内限定公開なので、複雑なポート自動検出は不要
- **修正**: 固定ポートの使用を検討

---

## 🔵 低優先度（アーキテクチャ改善）

### 7. プラットフォーム抽象化
**ファイル**: `projects/Game/src/scenes/Matching.cpp` (line 4)
- **問題**: winsock を直接使用しており、Linux や macOS で使えない可能性
- **修正**: 別のレイヤーを噛ませてプラットフォームを抽象化する（macOS で開発している人もいるため）

### 8. ネットワークアーキテクチャの改善
**ファイル**: `projects/Game/src/network/MultiplayerManager.cpp` (全体)
- **推奨事項**:
  1. 送受信するもののプロトコルを構造体で定義しておく
  2. ダメージ計算などはすべてホスト側に持たせて、クライアント側では演出だけやる方が良い
  3. セッション回り、切断の検知をちゃんと管理する

---

## 📝 レビュワーからのメッセージ

> 多分マッチングシステム書き直す際に色々と変更があると思うのでその際にコメントを参考にしてちまちま修正してほしいです。
> 意味が分からなかったり、実装の意図が食い違っていたら教えてください。

---

## ✅ 完了したタスク
（ここに完了したタスクを移動してください）

